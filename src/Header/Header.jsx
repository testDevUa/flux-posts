import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header>
            <nav>
                <a href="#">Posts</a>
                <a href="#">Projects</a>
                <a href="#">About us</a>
            </nav>
        </header>
    )
}

export default Header;